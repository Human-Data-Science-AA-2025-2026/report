\section{Risultati}\label{sec:risultati}
Le analisi restituiscono un quadro coerente nelle due direzioni previste
dai test: esiste un bias sistematico nei volumi e un disallineamento
strutturato nella distribuzione temporale.

\subsection{Entità del Bias: Test di Wilcoxon}
Sul dataset ($N = 40$) il test restituisce $W = 34.0$ con
$p = 4.3 \times 10^{-7}$. Il p-value è inferiore a tutte le soglie
considerate ($\alpha = 0.05, 0.01, 0.001$): \textbf{si rigetta $H_0$}
al livello più rigoroso.
Il gap mediano per singola combinazione paese-anno è di $918.5$ unità.

La verifica di robustezza aggrega i flussi per paese sull'intero
quinquennio ($N = 8$) e produce $W = 0.0$ con $p = 0.0078$.
Il valore $W = 0.0$ indica che tutti e otto i paesi esaminati presentano, senza
eccezioni, un gap di segno positivo (Eurostat $>$ ISTAT):
\textbf{si rigetta $H_0$} a $\alpha = 0.05$ e $\alpha = 0.01$, mentre
a $\alpha = 0.001$ il test non rigetta, a causa della bassa potenza statistica a $N = 8$.
Il gap mediano cumulato per paese nel quinquennio è di $4565.5$ unità.
I dati aggregati per paese sono visibili nella Figura~\ref{fig:table}, inoltre, la heatmap in Figura~\ref{fig:heatmap} mostra i gap percentuali paese per paese ed anno per anno.

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{scripts/figures/fig_table}
    \caption{Dati aggregati per paese.}
    \label{fig:table}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{scripts/figures/fig_heatmap}
    \caption{Heatmap rappresentante la percentuale del gap fra le cancellazioni delle residenze rilevate dall'istat e le immigrazioni nei paesi ospitanti registrate dall'eurostat}
    \label{fig:heatmap}
\end{figure}

\subsection{Struttura del Bias: Test del Chi-Quadrato}
Il test è applicato separatamente agli otto paesi ($k = 5$ anni).
In sette casi su otto il p-value risulta inferiore
a $0.001$: \textbf{si rigetta $H_0$} a ogni livello di significatività
considerato. Le anomalie più marcate si registrano per la Spagna
($\chi^2 = 1814.088$), la Germania ($\chi^2 = 691.664$) e la Francia
($\chi^2 = 454.519$).

L'unica eccezione è la Finlandia ($\chi^2 = 16.758$): il test rigetta
$H_0$ a $\alpha = 0.05$ e $\alpha = 0.01$, ma non a $\alpha = 0.001$.
Il profilo cronologico finlandese è l'unico che si avvicina a quello
di una sottostima proporzionalmente costante nel tempo.

Per i restanti sette paesi il tasso di mancata registrazione varia
da un anno all'altro: il ritardo anagrafico non è un offset fisso,
ma un fenomeno con dinamica propria.

Il trend aggregato di tutti i paesi è visibile in Figura~\ref{fig:trend}, inoltre, la media dei gap è visibile in Figura~\ref{fig:gap_by_contry} è stato anche aggiunto un grafico boxplot che aiuta a capire come sono sparsi i dati intorno la media, grafico in Figura~\ref{fig:boxplot}


\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{scripts/figures/fig_trend}
    \caption{Trend dei flussi migratori aggregati (2017-2021):
    l'area ombreggiata quantifica il divario tra le registrazioni
    Eurostat (superiori) e le cancellazioni ISTAT.}
    \label{fig:trend}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{scripts/figures/fig_boxplot}
    \caption{Boxplot della distribuzione del gap per paese: evidenzia
    le asimmetrie tra stati e l'effetto differenziale delle procedure
    anagrafiche locali.}
    \label{fig:boxplot}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{scripts/figures/fig_gap_by_country}
    \caption{Boxplot della distribuzione del gap per paese: evidenzia
    le asimmetrie tra stati e l'effetto differenziale delle procedure
    anagrafiche locali.}
    \label{fig:gap_by_contry}
\end{figure}